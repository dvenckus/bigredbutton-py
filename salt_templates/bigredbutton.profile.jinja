export VIRTUAL_ENV={{ virtualenv }}

bigredbutton_activate() {
    source {{ virtualenv }}/bin/activate #"$@"
}
export -f bigredbutton_activate

export bigredbutton_site_dir="{{ bigredbutton_site_dir }}"


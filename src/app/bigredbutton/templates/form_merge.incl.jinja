<form id="MergeForm" action="/merge" method="POST">
  <div class="control-group col-sm-4 float-left">
    <select id="mergeRepo" name="mergeRepo" class="form-control mergeRepo">
      {% for key in repositories['list_order'] -%}
      {% set repo = repositories['list'][key] -%}
      <option value="{{ key }}"{% if repo['attributes'] is defined %}{{ repo['attributes'] }}{% endif %}>{{ repo['name'] }}</option>
      {% endfor -%}                  
    </select>
  </div>
  <div class="control-group col-sm-4 float-left">
    <select id="mergeBranch" name="mergeBranch" class="form-control mergeBranch">
      {% for key in branches['list_order'] -%}
      {% set branch = branches['list'][key] -%}
      <option value="{{ key }}"{% if branch['attributes'] is defined %}{{ branch['attributes'] }}{% endif %}>{{ branch['name'] }}</option>
      {% endfor -%}                  
    </select>
  </div>
  <div class="actions col-sm-4 float-left">
    <input id="btnMerge" type="button" name="button"
           accept="" value="Merge" class="button-primary medium-button gradient-blue"/>
    <a href="#" id="cancelMerge" class="clearFormLink"><i class="glyph-cancel fas fa-times"></i></a>
    <div class="form-check testmode">
      <input class="form-check-input" type="checkbox" name="mergeTest" id="mergeTest" value="test" checked>
      <label class="form-check-label" for="mergeTest">
        Test Mode (No Changes)
      </label>
    </div>
  </div>

</form>



